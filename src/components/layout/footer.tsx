export function Footer() {
  return (
    <footer className="border-t border-hairline mt-16">
      <div className="mx-auto max-w-5xl px-4 py-8 flex flex-col sm:flex-row items-center justify-between gap-3 text-xs text-muted">
        <span className="font-display italic text-sm text-muted/80">
          Every big app starts with a spark.
        </span>
        <span className="font-mono">Spark — a portfolio project</span>
      </div>
    </footer>
  );
}
